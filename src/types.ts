export type BlogOutputType = {
  id: number;
  name: string;
  description: string;
  websiteUrl: string;
};

export type BlogInputType = {
  name: string;
  description: string;
  websiteUrl: string;
};
